name: Sync to Hugging Face Spaces

on:
  workflow_dispatch:

jobs:
  sync-to-hub:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled notice
        run: echo "HF sync is disabled; no push will be made."
